interface:
  display_name: "Markdown to Feishu"
  short_description: "Publish image Markdown into Feishu Docs."
  default_prompt: "Use $md-to-feishu-doc to upload this Markdown report with images into a Feishu cloud document."

policy:
  allow_implicit_invocation: true
